query {
  gliders {
	  registrationCode,
    name,
    flights {
      flightStatus
    }
  }
}

query {
  glider(id: 2) {
	  registrationCode,
    name,
    flights {
      flightStatus
    }
  }
}

query {
  pilots {
    firstName,
    lastName,
    birthDate,
    hasValidLicense,
    isStudent,
    isInstructor,
    firstPilotFlights {
      aircraft {
        registrationCode,
		name
      },
      takeOffTime,
      landingTime,
      flightStatus
    },
    secondPilotFlights {
      aircraft {
		registrationCode,
        name
      },
      takeOffTime,
      landingTime,
      flightStatus,
      hasInstructorGroundSupervision
    }
  }
}

query {
  pilots(where:  {
     firstName:  {
        startsWith: "Ar"
     }
  }) {
    birthDate
    email
    firstName
    hasInsurance
    hasValidLicense
    hasValidMedicalExamination
    id
    isInstructor
    isStudent
    lastName
  }
}

query {
  aircrafts(order: [ {
     productionYear: ASC
  }]) {
    id
    isAvailable
    isServiceRequired
    manufacturer
    name
    productionYear
    registrationCode
    seatsNumber
    stallSpeed
    topSpeed
    type
  }
}